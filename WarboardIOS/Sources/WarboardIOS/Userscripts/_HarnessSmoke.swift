// Temporary smoke marker to prove the headless SwiftPM target compiles +
// `swift test` runs on Linux. Removed once the first real engine source lands.
enum _HarnessSmoke {
    static let ready = true
}
